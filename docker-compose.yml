services:
  postgres:
    image: postgres:16
    restart: always
    container_name: postgres-petbook
    networks:
      - compose-bridge
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: senha
      POSTGRES_DB: petbook

networks:
  compose-bridge:
   driver: bridge

